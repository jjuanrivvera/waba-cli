coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 5%
comment:
  layout: "reach, diff, files"
ignore:
  - "tools/gendocs/**"   # generator with no runtime behaviour to protect
